# LabController

GitLab event controller with a runner-protocol-compatible CI executor and an MCP control plane, for Apple platforms.

Watches a GitLab instance, turns repository activity into structured events, runs CI jobs in ephemeral Apple-silicon VMs, and exposes status and events to LLM agents over MCP.

> Early WIP — API and scope are still settling.
